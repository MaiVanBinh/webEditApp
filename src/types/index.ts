export interface ButtonMenuProps {
  selectedTab: string;
  onSelectTab: (tab: string) => void;
}

export interface TabOneProps {
  // Define any props needed for TabOne
}

export interface TabTwoProps {
  // Define any props needed for TabTwo
}
